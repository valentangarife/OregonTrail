<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="ui.DialogoViewController"
      spacing="15"
      style="-fx-background-color: rgba(44, 62, 80, 0.95); -fx-padding: 20; -fx-background-radius: 10; -fx-border-color: #ecf0f1; -fx-border-width: 2; -fx-border-radius: 10;"
      maxWidth="500">

    <!-- Nombre del NPC -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Label fx:id="nombreNPCLabel" text="Comerciante"
               style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #f39c12;"/>
        <Label fx:id="tipoNPCLabel" text="[Comerciante]"
               style="-fx-font-size: 14px; -fx-text-fill: #95a5a6;"/>
    </HBox>

    <Separator style="-fx-background-color: #ecf0f1;"/>

    <!-- Área de diálogo -->
    <ScrollPane fitToWidth="true" prefHeight="150" style="-fx-background: transparent; -fx-background-color: transparent;">
        <VBox spacing="10" style="-fx-padding: 10;">
            <Label fx:id="dialogoLabel" text="Cargando diálogo..."
                   wrapText="true"
                   style="-fx-font-size: 16px; -fx-text-fill: #ecf0f1; -fx-line-spacing: 5px;"/>
        </VBox>
    </ScrollPane>

    <!-- Indicador de carga -->
    <HBox fx:id="loadingBox" alignment="CENTER" spacing="10" visible="false">
        <ProgressIndicator prefWidth="30" prefHeight="30"/>
        <Label text="Generando diálogo con IA..."
               style="-fx-font-size: 14px; -fx-text-fill: #3498db;"/>
    </HBox>

    <Separator style="-fx-background-color: #ecf0f1;"/>

    <!-- Botones de acción -->
    <HBox spacing="15" alignment="CENTER">
        <Button text="💬 Hablar" onAction="#hablarConNPC"
                style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-background-radius: 5;"/>
        
        <Button text="💡 Pedir Consejo" onAction="#pedirConsejo"
                style="-fx-background-color: #2ecc71; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-background-radius: 5;"/>
        
        <Button text="✖ Cerrar" onAction="#cerrarDialogo"
                style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 10 20; -fx-background-radius: 5;"/>
    </HBox>

</VBox>
